//Program to define functional interface (only one abstract method)
package com.tnsif.daysix.abstraction.interfaces;

@FunctionalInterface
public interface Cube {
	float calculate(int no);
	//float calculateSqauare(int no);
}
